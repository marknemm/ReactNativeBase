import { makeStyles } from '@rneui/themed';

/**
 * Gets the styles for the `UpdatePasswordScreen`.
 */
export const useStyles = makeStyles(() => ({
  formError: {
    marginTop: 10,
    textAlign: 'center',
  },
}));
