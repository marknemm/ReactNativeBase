import { StyleSheet } from 'react-native';

/**
 * The login screen styles.
 */
export const styles = StyleSheet.create({
  formError: {
    marginTop: 10,
    textAlign: 'center',
  },
  skipLogin: {
    marginTop: 'auto',
  },
});
