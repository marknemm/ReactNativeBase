import { createContext } from 'react';

/**
 * A context that provides the {@link Types.Navigation.RootNavigationContainerRef RootNavigationContainerRef}.
 *
 * @type {React.Context<Types.Navigation.RootNavigationContainerRef>}
 */
export const RootNavigationContainerRefContext = createContext(undefined);
