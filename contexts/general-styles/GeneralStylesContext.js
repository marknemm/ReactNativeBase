import { createContext } from 'react';

/**
 * A context that provides the {@link Types.Styles.GeneralStyles GeneralStyles}.
 *
 * @type {React.Context<Types.Styles.GeneralStyles>}
 */
export const GeneralStylesContext = createContext(undefined);
