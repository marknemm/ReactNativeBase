import { ImageStyles } from '@interfaces/styles';
import { ThemeWithColors } from '@interfaces/theme';

/**
 * Creates general {@link ImageStyles}.
 *
 * @param theme The {@link ThemeWithColors Theme}.
 * @returns The {@link ImageStyles}.
 */
export function createImageStyles(theme: ThemeWithColors): ImageStyles {
  return {};
}
