import { TooltipProps } from '@rneui/themed';

/**
 * The `Tooltip` component properties.
 */
export interface Props extends TooltipProps {

  /**
   * The children elements.
   */
  children?: React.ReactNode;

}
